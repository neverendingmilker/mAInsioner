<%- include('partials/head') %>
<div class="login-page">
  <div class="login-card">
    <div class="brand">mAInsioner</div>
    <p><%= typeof message !== 'undefined' && message ? message : 'Your Discord account has neither the "Administrator" permission nor the configured Mod role on any server the bot is in.' %></p>
    <a class="btn-discord" href="/logout">Log out</a>
  </div>
</div>
<%- include('partials/footer') %>
